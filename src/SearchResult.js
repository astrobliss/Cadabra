class SearchResult {
    constructor(name, price, site, url, imageURL) {
        this.name = name;
        this.price = price;
        this.site = site;
        this.url = url;
        this.imageURL = imageURL;
    }
}

export default SearchResult;